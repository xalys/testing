<header class="header">
    <div class="container">
        <div class="top-menu">
            <ul>
                <li class="top-menu-item"><a href="{{ path('main_index') }}">Home</a></li>
                {% if app.user %}
                    <div class="top-menu-item">
                        <a href="{{ path('fos_user_profile_show') }}">{{ app.user }}</a>
                    </div>
                        <div class="top-menu-item"><a href="{{ path('fos_user_security_logout') }}">Logout</a></div>

                {% else %}
                    <div class="top-menu-item"><a href="{{ path('fos_user_security_login') }}" class="a_menu">Log In</a>
                    </div>
                    <div class="top-menu-item"><a href="{{ path('fos_user_registration_register') }}"
                                                  class="green_box_link">Create account</a></div>
                {% endif %}
            </ul>
        </div>
    </div>
</header>